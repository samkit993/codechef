#include<iostream>
#include<algorithm>
#include<cstdio>
#include<string>
#include<vector>
#include<stack>
#include<queue>
#include<map>
using namespace std;

class Stack {
public:
    // Push element x onto stack.
    queue<int> q;
    void push(int x) {
        q.push(x);     
    }
    // Removes the element on top of the stack.
    void pop() {
      q.back()  
    }
    // Get the top element.
    int top() {
        
    }
    // Return whether the stack is empty.
    bool empty() {
        
    }
};
int main(){
	return 0;
}
